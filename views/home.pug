extends layouts/main
include mixins/videoBlock

block content
    each item in videos
        each item in videos
            +videoBlock({
                id : item.id,
                title : item.title,
                videoFile : item.videoFile,
                views : item.views
            })
            


